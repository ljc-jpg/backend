简介
zuul-server   网关             
cas-server    单点登录  登录   
eureka-server  注册中心
config-server    配置中心

启动顺序
euraka(注册) ->config(注册配置中心) ->zuul（注册网关）->(cas)登录验证 -> 业务服务集群

请求顺序 
ngix->zuul(转发) ->cas (合法验证)  -> euraka(获取服务) -> 业务服务集群
